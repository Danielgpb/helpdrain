{
  "name": "helpdrain",
  "version": "1.0.0",
  "private": true,
  "description": "Site statique helpdrain.be — build sans dépendance",
  "scripts": {
    "build": "node build.js",
    "check": "node scripts/check-links.js && node scripts/check-content.js",
    "serve": "npx serve dist",
    "images": "bash scripts/optimiser-images.sh"
  }
}
